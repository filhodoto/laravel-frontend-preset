import axios from 'axios';
import {API_ROUTE_TRANSLATIONS} from "../../config/ApiRoutesConfig";

/**
 * Store module state.
 */
const state = {
};

/**
 * Module actions.
 */
const actions = {
};

/**
 * Module mutations.
 */
const mutations = {
};

/**
 * Module getters.
 */
const getters = {
};

/**
 * Export store module.
 */
export default {
    state,
    actions,
    mutations,
    getters,
};